# MBMC AI Workshop — Python dependencies
# Install:  pip install -r requirements.txt
#
# NOTE on CPU PyTorch: sentence-transformers needs PyTorch. The default PyPI
# torch wheel bundles CUDA (~2.5 GB). The line below pulls the lightweight
# CPU-only wheel from PyTorch's CPU index instead. (Targets Linux/WSL2 — on
# macOS use plain `torch` from PyPI, which is already CPU/MPS.)
--extra-index-url https://download.pytorch.org/whl/cpu

# --- Core: LLM + LangChain ---
langchain==1.3.4
langchain-groq==1.1.2
langchain-community==0.4.2

# --- Module 2: RAG (embeddings + vector store) ---
torch==2.12.0+cpu              # CPU-only build (see note above)
sentence-transformers==5.5.1   # local HuggingFace embedding models
langchain-huggingface==1.2.2   # HuggingFaceEmbeddings wrapper for LangChain
faiss-cpu==1.14.2              # FAISS vector store (CPU build)

# --- Document loading / utilities ---
beautifulsoup4==4.15.0         # HTML parsing (bs4)
requests==2.34.2
python-dotenv==1.2.2           # load GROQ_API_KEY from .env